apiVersion: v1
kind: Service
metadata:
  name: sentia-voice-box
  namespace: openclaw
spec:
  selector:
    app: sentia-voice-box
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
